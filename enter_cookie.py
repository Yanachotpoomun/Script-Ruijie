from getcookie import get_cookie_name, get_cookie_value, get_url

def enter_cookie():
    cookie_name = get_cookie_name
    cookie_value = get_cookie_value
    url = get_url
    return cookie_name, cookie_value, url
